name: release
on:
  push:
    tags:
      - "v*.*.*"

env:
  ZIGBUILD_GLIBC: "2.28"
jobs:
  release:
    runs-on: >-
      ${{  
        contains(matrix.target, 'apple') && 'macos-latest' ||
        contains(matrix.target, 'msvc') && 'windows-latest' ||
        'ubuntu-22.04'
      }}
    strategy:
      matrix:
        target:
          # cross
          - x86_64-unknown-linux-gnu
          - x86_64-unknown-linux-musl
          - x86_64-pc-windows-gnu
          - x86_64-linux-android
          - aarch64-unknown-linux-gnu
          - aarch64-unknown-linux-musl
          - aarch64-linux-android
          - arm-unknown-linux-gnueabi
          - arm-unknown-linux-gnueabihf
          - arm-unknown-linux-musleabi
          - arm-unknown-linux-musleabihf
          - armv7-unknown-linux-gnueabi
          - armv7-unknown-linux-gnueabihf
          - armv7-unknown-linux-musleabi
          - armv7-unknown-linux-musleabihf
          # mips
          - mips-unknown-linux-gnu
          - mips-unknown-linux-musl
          - mipsel-unknown-linux-gnu
          - mipsel-unknown-linux-musl
          - mips64-unknown-linux-gnuabi64
          - mips64-unknown-linux-muslabi64
          - mips64el-unknown-linux-gnuabi64
          - mips64el-unknown-linux-muslabi64
          # windows
          - x86_64-pc-windows-msvc
          - aarch64-pc-windows-msvc
          # apple
          - x86_64-apple-darwin
          - aarch64-apple-darwin
          - aarch64-apple-ios
    steps:
      - uses: actions/checkout@v6
      - run: mkdir -p out/${{ matrix.target }} && mkdir -p out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}
      # cross
      - name: build cross
        if: |
          !contains(matrix.target, 'apple') &&
          !contains(matrix.target, 'msvc')
        uses: ./.github/actions/build/cross
        with:
          mode: cross
          target: ${{ matrix.target }}
          outdir: out/${{ matrix.target }}
      # cross-zigbuild-glibc
      - name: try build cross-zigbuild-glibc
        if: contains(matrix.target, 'linux') && contains(matrix.target, 'gnu')
        continue-on-error: true # may fail on some targets due to compiler-rt issues
        uses: ./.github/actions/build/cross
        with:
          mode: zigbuild
          zigbuild-glibc: ${{ env.ZIGBUILD_GLIBC }}
          target: ${{ matrix.target }}
          outdir: out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}
      # windows
      - name: build windows
        if: contains(matrix.target, 'msvc')
        uses: ./.github/actions/build/windows
        with:
          target: ${{ matrix.target }}
          outdir: out/${{ matrix.target }}
      # apple
      - name: build apple
        if: contains(matrix.target, 'apple')
        uses: ./.github/actions/build/apple
        with:
          target: ${{ matrix.target }}
          outdir: out/${{ matrix.target }}
      # finalize
      - name: gzip binaries
        run: |
          tar -C out/${{ matrix.target }} -zcf out/${{ matrix.target }}/realm-${{ matrix.target }}.tar.gz realm
          tar -C out/${{ matrix.target }} -zcf out/${{ matrix.target }}/realm-slim-${{ matrix.target }}.tar.gz realm-slim
          [ -f out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}/realm ] && \
            tar -C out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }} -zcf out/${{ matrix.target }}/realm-${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}.tar.gz realm
          [ -f out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}/realm-slim ] && \
            tar -C out/${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }} -zcf out/${{ matrix.target }}/realm-slim-${{ matrix.target }}-glibc${{ env.ZIGBUILD_GLIBC }}.tar.gz realm-slim
      - name: release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: out/${{ matrix.target }}/*.tar.gz
